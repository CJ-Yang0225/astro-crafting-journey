---
description: 這是別人開源的 Astro Theme 專案，我想要以它為基礎，建立自己的 Blog 去展示文章及作品
globs: 
---
# Astro Blog Rules

這是別人開源的 Astro Theme 專案，我想要以它為基礎，建立自己的 Blog 去展示筆記、文章及開發的作品。

作品可能包含用 React 或 Vue 框架開發出的程式碼，可能是 React-Three-Fiber (three.js) 的東西、Vue 製作的 UI Components，所以才挑選 Astro

- 前端網站主要會有 Home 頁面，展示「編譯邏輯與程式碼的 commit，封存從靈感到部署的記憶，開發旅途中各式札記，刻劃著我持續淬鍊的工匠之心。」Slogan，並列出網站會有的 pages 和 my works ([nav-menu.ts](mdc:src/config/nav-menu.ts))
- 語言以 TypeScript 撰寫，元件的 props 或需要繼承 (extends) 的地方以 interface 為主；其他地方可以看情境使用 type；如果有狀態是用字串區分，可以用 enum 命名像是：
  ```ts
  enum Status = {
    ADD = 0,
    EDIT = 1,
    READ_ONLY = 2
  }
  ```
- 一般檔案命名以 kebab-case 為主，像是 my-component.astro、app-input.tsx
- 不變的常數 const 可以放到 src/constants 裡面整理，命名像是這樣 `const API_BASE_URL="my-api-base-url"`
- 保持彈性與可擴充性，後續可以串接 Golang 開發的 API，做到點讚、留言、寄信通知等等功能